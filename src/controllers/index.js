const RoleController = require('./role');
const UserController = require('./user');
const PostController = require('./post');

module.exports = {
    RoleController,
    UserController,
    PostController,
};